import React from 'react';
import { View, Text } from 'react-native';

const EchangesScreen = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text>Échanges</Text>
  </View>
);

export default EchangesScreen;